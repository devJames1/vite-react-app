npm create vite@latest
 cd vite-react
 npm i

npm run dev (to run dev server). shows the default vite react app.

npm run build (to bundle for production).


Deploying your site using Cloudflare Pages:
 click on the branch icon by the side of vscode
 then click initialize Repository
 put descriptive message of commit at the top where it sayd message e.g First commit
 